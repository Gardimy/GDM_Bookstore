import { createSlice } from '@reduxjs/toolkit';

const initialState = 'construction';

const categoriesSlice = createSlice({
  name: 'categories',
  initialState,
  reducers: {
  },
});

export default categoriesSlice;
